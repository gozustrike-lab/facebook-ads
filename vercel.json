{
  "buildCommand": "prisma generate && (prisma db push --accept-data-loss 2>&1 || echo '⚠️ db push failed, tables will be created on first API call') && next build",
  "installCommand": "bun install",
  "framework": "nextjs",
  "regions": ["iad1"]
}
