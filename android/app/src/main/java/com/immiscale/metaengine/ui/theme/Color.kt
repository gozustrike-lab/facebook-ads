package com.immiscale.metaengine.ui.theme

import androidx.compose.ui.graphics.Color

// =============================================
// PALETA ULTRA-PREMIUM — IMMISCALE V5
// =============================================

// Fondo / Superficies
val White = Color(0xFFFFFFFF)
val SurfaceLight = Color(0xFFF8FAFC)
val SurfaceCard = Color(0xFFFFFFFF)
val SurfaceElevated = Color(0xFFF1F5F9)

// Texto
val BlackExecutive = Color(0xFF0F172A)
val TextSecondary = Color(0xFF475569)
val TextTertiary = Color(0xFF94A3B8)

// Primario — Verde Esmeralda Profundo
val EmeraldDeep = Color(0xFF059669)
val EmeraldMedium = Color(0xFF10B981)
val EmeraldSoft = Color(0xFFD1FAE5)
val EmeraldMint = Color(0xFFECFDF5)

// Acento — Azul Meta Oficial (exclusivo para API/Sync)
val MetaBlue = Color(0xFF1877F2)
val MetaBlueSoft = Color(0xFFE7F0FF)
val MetaBlueDark = Color(0xFF1565D8)

// Estados
val Success = Color(0xFF059669)
val Warning = Color(0xFFD97706)
val WarningSoft = Color(0xFFFFF7ED)
val Error = Color(0xFFDC2626)
val ErrorSoft = Color(0xFFFEF2F2)
val Info = Color(0xFF0284C7)

// Tendencias
val TrendUp = Color(0xFF059669)
val TrendDown = Color(0xFFDC2626)

// Bordes
val BorderLight = Color(0xFFE2E8F0)
val BorderMedium = Color(0xFFCBD5E1)

// Gradientes
val EmeraldGradientStart = Color(0xFF059669)
val EmeraldGradientEnd = Color(0xFF10B981)
val MetaGradientStart = Color(0xFF1877F2)
val MetaGradientEnd = Color(0xFF42A5F5)
